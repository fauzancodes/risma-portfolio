import { Allura, Poppins } from 'next/font/google';

export const poppins = Poppins({ subsets: ['latin'], weight: ['400', '500', '600', '700', '900'] });
export const allura = Allura({ subsets: ['latin'], weight: ['400'] });